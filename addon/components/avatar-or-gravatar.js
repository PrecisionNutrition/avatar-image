import Component from '@ember/component';
import layout from '../templates/components/avatar-or-gravatar';

export default Component.extend({
  // size (required): number
  // user (required): DS.Model
  // alt (optional): string
  // avatarIconClass (optional): string
  // avatarImageClass (optional): string

  layout,
});
